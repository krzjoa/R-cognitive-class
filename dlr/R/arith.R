# Arithmetic operations
# Ops numbers not working correctly
#' @examples
#' library(dlr)
#' x <- cpu_tensor(5, dims = 1)
#' y <- x ** 3
#' get_inputs(x)
#'
#' .get_r_ops(get_context(), 4)
#' y
# backward(y)
